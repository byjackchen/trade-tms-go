# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib
/trade-tms-go
/bin/
bin/

# Scratch / reference outputs
tmp/

# Test & coverage
*.test
*.out
coverage.*

# Go workspace
go.work
go.work.sum

# Env & secrets
.env
.env.local
# moomoo OpenD credentials (mounted into tms-live at runtime; never committed)
/secrets/*
!/secrets/*.example

# Editor / OS
.idea/
.vscode/
.DS_Store

# built binary at repo root
/tms
# playwright-mcp tool cruft
.playwright-mcp/

# backtest run artifact output (DB is source of truth)
/runs/

# stray gate/e2e screenshots at repo root
/*.png

# keep secret templates, never real secrets
!secrets/*.example
